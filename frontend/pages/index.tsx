// import { useEffect, useState } from "react";
// import { connect, sendMsg } from "../services";

export default function Index() {
  // const [count, setCount] = useState(0);
  // const [events, setEvents] = useState<MessageEvent<string>[]>([]);
  //
  // useEffect(() => {
  //   connect((evt: MessageEvent<string>) => {
  //     setEvents((events) => [...events, evt]);
  //   });
  // }, []);
  //
  // const send = () => {
  //   sendMsg("Hello from React " + count);
  // };

  return (
    <>
      <h1>Chat App</h1>
    </>
  );
}
